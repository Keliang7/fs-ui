for i in components core docs hooks theme utils; do
  if [ ! -d "$i" ]; then
    echo "Directory $i does not exist. Creating..."
    mkdir $i
  fi
  cd $i
  echo "Initializing pnpm in $i"
  pnpm init --yes
  cd ..
done